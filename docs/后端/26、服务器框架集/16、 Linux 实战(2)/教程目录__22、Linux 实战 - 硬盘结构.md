# 22、Linux 实战 - 硬盘结构
- 来源：https://ddkk.com/zhuanlan/server/linux/5/22.html
- 分类：服务器框架
- 分组：教程目录
## 硬盘

### 磁盘结构

扇区是磁盘的最小存储单位，每个扇区的大小是固定的，为512Byte。硬盘里有多个磁盘，每个磁盘中，有多个同心圆，这些同心圆就是磁道，多个扇区的磁道叠在一起，就组成了柱面，扇区组成磁道，磁道组成了柱面。

### 硬盘接口

- IDE硬盘接口，并口，也称作"ATA硬盘"或"PATA硬盘"，是早期机械硬盘的主要接口，ATA133硬盘的理论速度可以达到133MB/s，现在已经被淘汰了。
- SATA接口，串口，是速度更高的硬盘标准，具备了更高的传输速度，并具备了更强的纠错能力。目前是SATA三代，理论传输速度达到了600MB/s。
- SCSI接口，小型计算机系统接口，广泛应用在服务器上，具有应用范围广、多任务、带宽大、CPU占用率低及支持热插拔等优点，理论传输速度为320MB/s。
- M.2接口，是Intel为超级本推出的一种替代MSATA新的接口规范。M.2接口有两种类型：Socket 2（B key——ngff）和Socket 3（M key——nvme），其中Socket2支持SATA、PCI-E X2接口，而如果采用PCI-E ×2接口标准，最大的读取速度可以达到700MB/s，写入也能达到550MB/s。而其中的Socket 3可支持PCI-E ×4接口，理论带宽可达4GB/s。
