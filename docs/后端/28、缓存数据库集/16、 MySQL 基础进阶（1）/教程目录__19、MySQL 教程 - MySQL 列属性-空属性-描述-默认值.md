# 19、MySQL 教程 - MySQL 列属性-空属性/描述/默认值
- 来源：https://ddkk.com/zhuanlan/db/mysql/5/19.html
- 分类：缓存数据库
- 分组：教程目录
真正约束一个字段的是数据类型，但是数据类型的约束很单一，有时候还需要有一些额外的约束，这些额外的约束就是列属性。列属性主要有:null/not null，default，primary key, unique key, auto_increment, comment等。所以，这篇我们来了解一下列的几个属性。

**空属性**

空属性有两个值：NULL(默认) NOT NULL(不为空)

虽然默认的，数据库基本都是字段为空，但是实际上在真实数据库设计的时候，尽可能的要保证所有的数据都不应该为空，因为空数据没有意义和空数据没有办法参与运算。

创建一个教室表举例：

上面创建表过程，我们约定了name的值不为空，class的值默认允许为空。下面来插入数据：

所以，当给name赋值为空插入，提示报错。

**列描述：comment**

描述：comment，没有实际含义，是专门用来描述字段，会根据表创建语句保存，用来给数据库管理员来进行了解的。例如新来一个运维或者开发接手项目，新来的可以通过commment的内容来接当前列字段是什么业务数据。

举例：新建一个教师表

上面的comment语句就是这样使用的，例如创建表的时候，工资对应的英文单词想不起来了，这哥们就写上了money，但是怕以后别人不懂什么意思，就用comment语句表明了这个字段是工资的意思。

如何看到到comment信息呢

**默认值**

默认值就是:某一种数据会经常性的出现某个具体的值。可以在一开始就制定好，在需要真实数据的时候，用户可以选择性的使用默认值。

使用方式：默认值的关键字default

创建一个表，年龄和性别设置default值

默认值生效的办法就是，在给插入数据的时候，不给该字段赋值就可以。

我在插入数据的时候，一共有三个字段，只给name字段赋值了张三，gender和age字段没有赋值，结果都采用默认值。

还有一种方式，直接在字段值位置上写default关键字，如下

这种方式建议使用，不用担心顺序问题和漏写了那个字段，遇到不确定的字段值就是有default关键字。
