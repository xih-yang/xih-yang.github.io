# 29、XML 范例
- 来源：https://ddkk.com/zhuanlan/xml/xml/29.html
- 分类：其他语言
- 分组：教程目录
下面这些范例演示了 XML 文件、XML 格式化和 XML 转换 ( XSLT )

还演示了与 XML 一起使用的 JavaScript ( AJAX )

### 查看 XML 文件

**1、** 查看一个简单的XML文件(note.xml)；

**2、** 查看一个带有错误的相同的XML文件；

**3、** 查看一个XML的CD目录；

**4、** 查看一个XML的植物目录；

**5、** 查看一个XML的食物菜单；

**范例解释**

### XML 和 CSS

**1、** 查看一个XML的CD目录；

**2、** 查看对应的CSS文件；

**3、** 显示由CSS文件格式化的CD目录；

**范例解析**

### XML 和 XSLT

**1、** 查看一个XML的食物菜单；

**2、** 查看对应的XSLT样式表；

**3、** 显示由XSLT样式表格式化的食物菜单；

**范例解析**

### 解析 XML 和 XML DOM

**1、** 查看一个简单的XML文件（note.xml）；

**2、** [解析XML文件-跨浏览器实例]/t/penglei/html/xml_parsertest)；

**3、** [解析XML字符串-跨浏览器实例]/t/penglei/html/xml_parsertest2)；

**范例解析**

### XML to HTML

**1、** 查看一个XML的CD目录；

**2、** [把XML数据显示为HTML表格]/t/penglei/html/xml_display_table)；

**范例解析**

### XML 应用程序

**1、** 查看一个XML的CD目录；

**2、** [在div元素中显示XML数据]/t/penglei/html/xml_app_navigate)；

**3、** [XML节点导航]/t/penglei/html/xml_app_navigate)；

**4、** [一个简单的CD目录应用程序]/t/penglei/html/xml_app)；

**范例解析**

### 来自服务器的 XML 输出

**1、** 查看ASP如何返回XML；

**2、** 查看PHP如何返回XML；

**3、** 查看来自数据库的XML输出；

**范例解析**

### XML DOM 高级

**1、** [获取XML元素的值]/t/penglei/html/xml_dom_getelement)；

**2、** [获取XML属性的值]/t/penglei/html/xml_dom_getattribute)；

**3、** [更改XML元素的值]/t/penglei/html/xml_dom_changeelement)；

**4、** [把一个新的属性添加到XML元素]/t/penglei/html/xml_dom_addattribute)；

**5、** [创建一个新的XML元素]/t/penglei/html/xml_dom_createelement)；

**6、** [删除一个XML元素]/t/penglei/html/xml_dom_removeelement)；

**范例解析**
