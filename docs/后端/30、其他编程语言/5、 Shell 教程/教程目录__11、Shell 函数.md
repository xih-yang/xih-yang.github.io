# 11、Shell 函数
- 来源：https://ddkk.com/zhuanlan/other/shell/11.html
- 分类：Shell 教程
- 分组：教程目录
有时候，一些代码总是重复的写一遍又写一遍，shell 能不能像其它语言那样可以定义函数呢？使用函数来封装一些代码

答案是很明显的，当然可以的。

linux shell 可以让用户自定义函数，然后可以在其它地方随便调用

### shell 中定义函数的语法如下

```sh
[ function ] funname [()]
{
    action;
    [return int;]
}
```

**1、** 可以带functionfunname()定义，也可以直接funname()定义,不带任何参数；

**2、** 参数返回，可以显示加return返回，如果不加，将以最后一条命令运行结果，作为返回值，return后跟数值n(0-255)；

#### 范例 1: 定义一个函数并调用它

```sh
#!/bin/bash
# author:DDKK.COM 弟弟快看，程序员编程资料站
# url:www.ddkk.com
my_first_fun() {
    echo "这是我们写的第一个 shell 函数!"
}
echo "-----函数开始执行-----"
my_first_fun
echo "-----函数执行完毕-----"
```

上面范例执行结果：

```sh
-----函数开始执行-----
这是我们写的第一个 shell 函数!
-----函数执行完毕-----
```

> 调用函数的时候不要添加小括号 ()， 我刚刚毫无意识的添加了之后就报错了

#### 范例 2: 定义一个带有 return 语句的函数

```sh
#!/bin/bash
# author:DDKK.COM 弟弟快看，程序员编程资料站
# url:www.ddkk.com
my_fun_with_return(){
    echo "这个函数会对输入的两个数字做加法运算..."
    echo "请输入第一个数字: "
    read aNum
    echo "请输入第二个数字: "
    read anotherNum
    echo "你输入的两个数字分别为: $aNum 和: $anotherNum !"
    return $(( $aNum + $anotherNum ))
}
funWithReturn
echo "你输入的两个数字之和为: $? !"
```

上面范例执行结果

```sh
这个函数会对输入的两个数字做加法运算...
请输入第一个数字:
88
请输入第二个数字:
16
你输入的两个数字分别为: 88 和: 16 !
你输入的两个数字之和为: 104 !
```

> 注意： 函数返回值可以在调用该函数后通过 $ ? 来获得
>
> 所有函数在使用前必须定义。 意味着必须将函数放在脚本开始部分，直至shell解释器首次发现它时，才可以使用 调用函数仅使用其函数名即可

## 函数参数

在Shell 中，调用函数时可以传递参数。

传递的参数，在函数体内部，可以通过 `$` n 的形式来获取参数的值

例如， `$` 1 表示第一个参数， `$` 2 表示第二个参数...

#### 范例 1 : 带参数的函数：

```sh
#!/bin/bash
# filename: demo.sh
# author:DDKK.COM 弟弟快看，程序员编程资料站
# url:www.ddkk.com
fun_with_param(){
    echo "第一个参数为: $1 "
    echo "第二个参数为: $2 "
    echo "第十个参数为: $10 "
    echo "第十个参数为: ${10} "
    echo "第十一个参数为: ${11} "
    echo "参数总数有 $# 个"
    echo "作为一个字符串输出所有参数 $* "
}
fun_with_param 1 3 5 7 9 11 13 17 19 23 29 # 基础调用函数不要使用小括号
```

执行范例输出结果

```sh
$ sh demo.sh
第一个参数为: 1
第二个参数为: 3 
第十个参数为: 10 
第十个参数为: 23 
第十一个参数为: 29 
参数总数有 11 个 
作为一个字符串输出所有参数 1 3 5 7 9 11 13 17 19 23 29 
```

`$` 10 不能获取第十个参数，获取第十个参数需要 `$` {10} 当 n>`=10 时，需要使用`$` {n} 来获取参数

### 下面几个特殊字符用来处理参数

参数处理
说明

 `$` #
传递到脚本的参数个数

 `$` *
以一个单字符串显示所有向脚本传递的参数

 `$` `$`
脚本运行的当前进程ID号

 `$` !
后台运行的最后一个进程的ID号

 `$` @
与 `$` *相同，但是使用时加引号，并在引号中返回每个参数

 `$` -
显示Shell使用的当前选项，与set命令功能相同

 `$` ?
显示最后命令的退出状态。0表示没有错误，其他任何值表明有错误
