一、Go 内核基础（底层夯实）
• Go 环境与工具：Go 1.22+/1.24 LTS、GOROOT/GOPATH、Go Modules、gofmt、goimports
• 基础语法全体系：

变量声明（var/:=）、零值、类型推断、类型转换

运算符、流程控制（if/for/switch/select）

数组与切片（array vs slice、扩容原理）、map、range

函数：多返回值、命名返回值、可变参数、defer、闭包
• 指针与内存：指针运算限制、new vs make、逃逸分析基础
• 结构体与方法：结构体定义、方法接收者（值/指针）、匿名字段与组合
• 接口：隐式实现、空接口、类型断言、type switch、接口内部表示
• 错误处理：error 接口、errors.Is/As、fmt.Errorf %w、panic/recover
• 包与模块：包导入路径、init()、internal、go mod 依赖管理
• 并发编程核心：

goroutine 原理（GPM 模型）、runtime 调度

channel（无缓冲/有缓冲）、select 多路复用、close 语义

sync 包：Mutex/RWMutex/WaitGroup/Once/Cond/Pool

context：超时/取消/传值、ctx 树形传递

原子操作：atomic 包、CAS
• 标准库深度：

fmt/strings/strconv/time/flag/log 高频库

io.Reader/Writer 体系、os/exec/signal
• Go 现代特性：

泛型（Go 1.18+）：类型约束、泛型函数/结构体、comparable

工作区模式（Go 1.18+ workspace）

循环变量语义变更（Go 1.22）

rangefunc/迭代器（实验性）

二、数据库与持久层（存储根基）
• SQL 数据库：

database/sql 接口、连接池（SetMaxOpenConns/MaxIdleConns）

驱动：go-sql-driver/mysql、pgx、sqlite3

ORM：GORM（模型定义、自动迁移、预加载、事务、Hook）

类型安全：sqlc（代码生成）、sqlx、ent（Facebook 图结构 ORM）
• MySQL 深度（在 Go 中的实践）：

复杂查询、Explain 分析、索引优化

事务隔离级别与错误重试

分库分表中间件对接（ShardingSphere-Go 等）
• PostgreSQL 深度：

pgx 连接池、COPY 协议、LISTEN/NOTIFY

JSONB 操作、数组、全文搜索

国产数据库基座适配（人大金仓 KingbaseES 基于 PG）
• NoSQL：

Redis：go-redis/v9、Redis Cluster/哨兵、分布式锁、缓存策略

MongoDB：mongo-go-driver、BSON、聚合管道

Elasticsearch：olivere/elastic、go-elasticsearch 官方客户端
• 向量数据库（AI 配套）：

Milvus-Go SDK、PGVector 通过 pgx 使用、Chroma-Go
• 数据库迁移：

golang-migrate、atlas、goose
• 补充（现代）：

信创数据库适配：达梦 DM8 Go 驱动探索、人大金仓 KingbaseES 兼容 PG 模式

分布式 SQL：TiDB Go 客户端、CockroachDB

三、Go Web 与框架生态（核心构建）
• 基础：net/http 标准库、ServeMux、Handler 接口
• 路由框架（选型）：

Gin：中间件、路由组、参数绑定、验证、渲染

Echo：极简、高性能、HTTP2 支持

Fiber（Fasthttp 驱动）：极致性能、与 Express 类似

Hertz：字节跳动云原生高性能框架（支持 HTTP3）
• 中间件设计：日志、认证、限流、重试、pprof
• 请求处理：参数校验（go-playground/validator）、序列化（json/proto）、响应规范
• 模板引擎：html/template、jet、pongo2
• 安全：JWT（golang-jwt）、OAuth2（golang.org/x/oauth2）、csrf、CORS
• 全栈/API 优先框架：

Buffalo（Web 全栈）

Goa（设计优先的 API 框架）

gRPC-Gateway（HTTP 转 gRPC）
• 补充（现代）：

OpenTelemetry 集成：otelgin、otelecho

响应式/流式：SSE、WebSocket（gorilla/websocket 或 nhooyr.io/websocket）

四、Go 工程化与测试（企业必备）
• 项目管理：

Go Modules、依赖版本管理、go mod tidy/vendor

多模块工作区（go.work）

内部包组织规范（cmd/internal/pkg）
• 代码规范：

golangci-lint 全项检查、revive、staticcheck

gofmt/goimports、uber-go 风格指南
• 代码生成：

go generate、stringer、mockgen、protoc-gen-go
• 测试体系：

单元测试：testing 包、表格驱动测试、子测试、覆盖率

断言库：testify/assert、goconvey、is

Mock：gomock、mockery、testify/mock

集成测试：testcontainers-go（数据库/Redis/MQ 容器化）

性能测试：benchmark、pprof（CPU/内存/goroutine 分析）、trace
• API 文档：

Swaggo（Swagger 生成）、OpenAPI 3.0

设计即文档（Goa、Huma）
• 日志体系：

slog（Go 1.21+ 结构化日志）、zap/zerolog

链路追踪：TraceID 注入、MDC 等价实现
• 补充（现代）：

变异测试：gremlins、go-mutesting

模糊测试：Go 1.18+ fuzzing（testing.F）

五、Go 中间件与分布式通信（分布式系统筋脉）
• 消息队列：

Kafka：sarama、confluent-kafka-go、消费者组、幂等生产

RocketMQ：go-rocketmq-client/v2（事务消息、顺序消息）

RabbitMQ：amqp091-go、重连机制

NATS：nats.go（云原生轻量 MQ、JetStream）

Apache Pulsar：pulsar-client-go
• 缓存中间件：

go-redis 进阶：Pipeline、Lua 脚本、分布式锁（Redlock）

本地缓存：freecache、bigcache、ristretto

缓存策略：防击穿 singleflight、缓存一致性与延迟双删
• API 网关 / 代理：

反向代理：net/http/httputil.ReverseProxy、Traefik（Go 编写）

网关框架：Krakend、Tyk、自家网关设计（基于 Gin/Hertz）
• RPC 框架：

gRPC：protobuf 定义、拦截器、负载均衡、超时重试

Go-Micro（v3）、Kitex（字节跳动高性能 RPC）

HTTP API 风格：Connect-Go（gRPC 兼容但基于 HTTP）
• 分布式协调：

etcd：etcd/clientv3、Leader 选举、Watch、分布式锁

ZooKeeper：go-zookeeper/zk

Consul、Nacos Go SDK
• 分布式调度：

asynq（基于 Redis 的任务队列）、Machinery、crontab 库
• 补充（现代）：

消息流融合：Kafka + Schema Registry（protobuf）

信创中间件：东方通 TongLink/Q Go 客户端开发趋势

六、Go 分布式系统与微服务（架构师核心）
• 微服务设计模式：

服务拆分、API 设计、BFF 模式

DDD 在 Go 中的实践（整洁架构、六边形架构）
• 服务注册与发现：

etcd、Consul、Nacos

Kubernetes Service（DNS 发现）
• 配置中心：

Viper（本地+远程）、Nacos Go SDK
• 远程调用与治理：

gRPC + 负载均衡（resolver + balancer）

超时、重试、熔断：go-resiliency、hystrix-go、gobreaker

限流：uber-go/ratelimit、golang.org/x/time/rate
• 分布式基础组件：

分布式 ID：snowflake（bwmarrin/snowflake）、sonyflake

分布式锁：Redis（redsync）、etcd（concurrency.STM）

分布式事务：Saga、TCC、DTM（分布式事务管理器，Go 实现）

幂等性设计
• 可观测性：

链路追踪：OpenTelemetry Go SDK，Jaeger/Zipkin/SkyWalking 导出器

指标：Prometheus client_golang、grafana

日志：zap/zerolog + Loki
• 服务网格：

Istio/Envoy 与 Go 微服务，Sidecar 模式

MOSN（蚂蚁金服 Go 实现 Sidecar）
• 补充（现代）：

eBPF 微服务观测（Go 程序零侵入）

七、Go 云原生与部署运维（生产落地）
• Go 在云原生中的地位：Kubernetes、Docker、Prometheus 均 Go 编写
• 容器化：

多阶段构建、静态编译、scratch 镜像

构建最小二进制（-ldflags、upx 压缩）

docker-compose 编排、健康检查
• Kubernetes：

client-go：Pod/Deployment/Service 操作

Controller-runtime / Kubebuilder：开发 Operator

CRD 自定义资源、Webhook

Helm Chart 编写 Go 应用部署包
• 云原生 Go 应用：

配置与环境变量注入

优雅关闭：signal.NotifyContext、server.Shutdown

探针实现（liveness/readiness）
• Serverless：

AWS Lambda Go runtime、阿里云函数计算 Go 支持

CloudEvents Go SDK、Knative Go 应用
• CI/CD：

GitHub Actions、GitLab CI 构建 Go 项目

交叉编译、产物发布

GoReleaser 自动化发布
• 可观测性：

Prometheus exporter 编写

日志收集（Fluent Bit/Filebeat）、OpenTelemetry 全链路
• 补充（现代）：

WASM/边缘计算：Go 编译为 WASI（tinygo）、Fermyon Spin

信创云底座：鲲鹏/飞腾 + 银河麒麟 + Go 应用性能测试

八、Go 性能调优、安全与架构设计（生产必备）
• 性能分析工具：

pprof CPU/内存/goroutine/block/mutex 分析

trace 调度跟踪、go-torch 火焰图

escape analysis 优化内存分配

并发竞态检测：go test -race
• Go 性能优化实战：

内存零分配：sync.Pool、复用缓冲区、strings.Builder

协程与通道优化：channel 大小、扇入扇出模式

序列化性能：json-iterator、easyjson、protobuf

HTTP 服务优化：连接池、超时控制、gzip 缓冲
• 安全防护：

SQL 注入（参数化查询）、命令注入、模板注入

JSON/XML 解析安全（防止 Billion Laughs）

认证授权最佳实践、JWT 刷新策略、RBAC/ABAC

供应链安全：依赖扫描（govulncheck）、模块代理校验
• 架构模式：

高可用：限流/熔断/降级/隔离

海量链接：C10K/C100K io_uring、epoll 事件驱动

架构演进：单体→微服务→事件驱动

反模式避免（goroutine 泄漏、context 不取消等）

九、信创国产化与 Go 生态适配（新护城河）
• 国产操作系统与硬件：

Go 交叉编译到 ARM64（鲲鹏/飞腾）、龙芯（go 支持 loong64）

在银河麒麟、统信 UOS、OpenEuler 上的构建与测试

与国产 JDK 对比，Go 静态编译跨平台优势
• 国产数据库适配：

人大金仓 KingbaseES（基于 PG）→ pgx 驱动兼容

达梦 DM8：ODBC/CGO 适配探索

OceanBase Go 客户端、GaussDB Go 驱动

中间件：东方通 TongWeb（Java）→ Go 通过 HTTP/gRPC 对接
• 信创迁移策略：

Go 服务替代 Java 服务在信创环境的价值（轻量、静态编译）

适配 UOS 安全基线、审计日志
• 补充（现代）：

自主可控云原生工具链：KubeEdge、Karmada（Go 开发，信创云核心）

十、Go + AI 全体系（未来核心）
• 大模型集成：

OpenAI Go SDK、langchaingo（Go 版 LangChain）

国内模型接入：文心一言 Go SDK、通义千问 DashScope SDK

Spring AI 对应：Go 中为 langchaingo + 自定义封装
• RAG 与向量数据库：

向量操作：gonum 计算余弦相似度、pgvector/pgx 查询

Milvus-Go SDK、Chroma-Go、Weaviate Go 客户端

RAG 工作流：文档加载（pdfgo、goquery）、分块、嵌入、检索
• AI Agent 开发：

langchaingo Agent/Tools 特性

ReAct 模式实现、多 Agent 协作

工具调用：构建 Function Calling 工具函数
• AI 微服务与推理：

部署模型推理服务：Go HTTP 封装的 Triton Inference Server 客户端

流式响应（SSE）到 ChatGPT 接口
• AI 辅助 Go 开发：

GitHub Copilot/Cursor 在 Go 项目的使用

AI 生成测试用例（gotests）、代码审查
• AI 运维：

Token 计费统计、LLM 调用监控埋点

输出质量评估与日志分析
• 补充（现代）：

MLOps Go 工具：Go 工程中训练管道调度

私有化部署模型对接 Go 服务

十一、Go 工具协作与职业成长（收尾提升）
• 文档与协作：

GoDoc/Go 标准注释、pkgsite

技术文档：Docusaurus、Hugo 静态站点（Go 编写）

图表：PlantUML、Mermaid（Hugo 集成）
• 版本与协作：

Git 最佳实践（Conventional Commits）

Code Review 要点、Gerrit/GitHub Flow

Go 依赖的 vendoring 与代理（GOPROXY）
• 面试成长：

Go 面试题库：GPM 调度、channel 关闭、slice 扩容、map 并发安全、逃逸分析

系统设计：秒杀、短链、IM 系统（Go 技术栈实现）

手写代码：实现 goroutine pool、限流器、simple cache
• 职业路线：

Go 初级：基础语法、标准库应用

Go 中级：微服务、中间件、性能调优

Go 高级/架构师：分布式系统设计、云原生基础设施、Operator 开发

专家：Kubernetes 生态贡献、高性能基础库开发、技术布道